# 生产环境构建（npm run build 时自动加载）
# 复制为 .env.production.local 并修改域名（.local 会覆盖本文件且通常不入库）

# 默认走站点同源地址；生产服务器需要把 API 路径反代到后端网关。
# 如果前端与网关不同域名，请复制为 .env.production.local 后填写真实地址。
REACT_APP_BASE_URL=

# 生产必须关闭 mock
REACT_APP_ENABLE_MOCK=false
