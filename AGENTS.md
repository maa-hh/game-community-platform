# 后端开发规则入口

本仓库的后端代码统一遵守 [`service/CODING_STANDARDS.md`](service/CODING_STANDARDS.md)。任何 AI 助手、开发者或 Code Review 在新增、修改、重构 Java、DTO/VO、Feign、SQL、配置和服务间协议前，必须先完整阅读该文件及目标服务附录。

强制执行：

1. 先确认模块边界和公开/内部接口边界，再开始写代码。
2. Controller 只做绑定、鉴权和转发；复杂查询、分页、筛选、批量请求和服务间命令使用语义化 DTO。
3. 内部数据库主键、中间件对象和内部凭证不得进入公开 DTO/VO、公开路由或公开 Service 接口。
4. 跨服务协议、事件、Redis 前缀和限流动作统一使用 `common.constant`；环境相关值使用配置文件。
5. 修改完成后用 `rg` 清理旧调用和死代码，并运行对应 Maven 测试；公共模块或服务间协议变更还要运行全仓编译。
6. 规则与实现不一致时，先更新规则或补充兼容边界说明，再继续新增代码。

`service/CODING_STANDARDS.md` 是详细规则的唯一维护位置；本文件只负责让规则在仓库根目录可发现、可执行。
